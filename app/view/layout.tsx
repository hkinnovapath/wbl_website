"use client";

// -----------Independent layout for Resume View----------- 
// --------------------------------------------------------
export default function ResumeViewLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {children}
        </div>
    );
} 